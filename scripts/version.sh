#!/bin/bash
set -x

PROG=rke2
REGISTRY=${REGISTRY:-docker.io}
REPO=${REPO:-rancher}
K3S_PKG=github.com/rancher/k3s
RKE2_PKG=github.com/rancher/rke2
GO=${GO-go}
GOARCH=${GOARCH:-$("${GO}" env GOARCH)}
ARCH=${GOARCH}
GOOS=${GOOS:-$("${GO}" env GOOS)}
if [ -z "$GOOS" ]; then
    if [ "${OS}" == "Windows_NT" ]; then
      GOOS="windows"
    else
      UNAME_S=$(shell uname -s)
		  if [ "${UNAME_S}" == "Linux" ]; then
			    GOOS="linux"
		  elif [ "${UNAME_S}" == "Darwin" ]; then
				  GOOS="darwin"
		  elif [ "${UNAME_S}" == "FreeBSD" ]; then
				  GOOS="freebsd"
		  fi
    fi
fi
GOOS=linux
UNAME_S=Linux
TREE_STATE=clean
COMMIT=$DRONE_COMMIT
if [ -z "${IMAGE_BUILD_VERSION}" ]; then IMAGE_BUILD_VERSION=multiarch-build$(date +%Y%m%d); fi
BASE_VERSION=${BASE_VERSION:-v1.16.10b7-multiarch}
KUBERNETES_VERSION=${KUBERNETES_VERSION:-v1.22.4}
RKE2_RELEASE=${RKE2_RELEASE:-rke2r1}
RKE2_MULTIARCH_RELEASE=${RKE2_MULTIARCH_RELEASE:-multiarch-alpha1}
VERSION=${VERSION:-$KUBERNETES_VERSION-$RKE2_MULTIARCH_RELEASE+$RKE2_RELEASE}
REVISION=$(git rev-parse HEAD)$(if ! git diff --no-ext-diff --quiet --exit-code; then echo .dirty; fi)
PLATFORM=${GOOS}-${GOARCH}
RELEASE=${PROG}.${PLATFORM}

# hardcode versions unless set specifically
KUBERNETES_IMAGE_TAG=${KUBERNETES_IMAGE_TAG:-$KUBERNETES_VERSION-$RKE2_RELEASE}
ETCD_VERSION=${ETCD_VERSION:-v3.5.0-k3s2}
PAUSE_VERSION=${PAUSE_VERSION:-3.6}
CCM_VERSION=${CCM_VERSION:-v0.0.3-build20211118}

if [ -d .git ]; then
    if [ -z "$GIT_TAG" ]; then
        GIT_TAG=$(git tag -l --contains HEAD | head -n 1)
    fi
    if [ -n "$(git status --porcelain --untracked-files=no)" ]; then
        DIRTY="-dirty"
        TREE_STATE=dirty
    fi
fi

K3S_VERSION=${K3S_VERSION:-v1.22.4-rc2-k3s1}
CONTAINERD_VERSION=${CONTAINERD_VERSION:-v1.5.7-k3s2}
CRICTL_VERSION=${CRICTL_VERSION:-v1.22.0}
RUNC_VERSION=${RUNC_VERSION:-v1.0.2}

COREDNS_VERSION=${COREDNS_VERSION:-v1.8.5}
METRICS_SERVER_VERSION=${METRICS_SERVER_VERSION:-v0.5.2}
KLIPPER_HELM_VERSION=${KLIPPER_HELM_VERSION:-v0.6.8}
NGINX_INGRESS_VERSION=${NGINX_INGRESS_VERSION:-1.0.2-multiarch-hardened2}
NGINX_INGRESS_DEFAULT_BACKEND_VERSION=${NGINX_INGRESS_DEFAULT_BACKEND_VERSION:-1.5-rancher1}
HARDENED_CALICO_VERSION=${HARDENEDCALICO_VERSION:-v3.20.2}
CALICO_VERSION=${CALICO_VERSION:-v3.20.2}
CALICO_OPERATOR_VERSION=${CALICO_OPERATOR_VERSION:-v1.20.4}
CALICO_CRD_VERSION=${CALICO_CRD_VERSION:-v1.0.202}
FLANNEL_VERSION=${FLANNEL_VERSION:-v0.15.1}
CILIUM_VERSION=${CILIUM_VERSION:-v1.10.4}
CILIUM_STARTUP_SCRIPT_VERSION=${CILIUM_STARTUP_SCRIPT_VERSION:-62bfbe88c17778aad7bef9fa57ff9e2d4a9ba0d8}
MULTUS_VERSION=${MULTUS_VERSION:-v3.8}
CNI_PLUGINS_VERSION=${CNI_PLUGINS_VERSION:-v1.0.1}
SRIOV_VERSION=${SRIOV_VERSION:-v1.0.0}
SRIOV_DEVICE_PLUGIN_VERSION=${SRIOV_DEVICE_PLUGIN_VERSION:-v3.3.2}
SRIOV_CNI_VERSION=${SRIOV_CNI_VERSION:-v2.6.1}
SRIOV_RESOURCES_INJECTOR_VERSION=${SRIOV_RESOURCES_INJECTOR_VERSION:-v1.2}
VSPHERE_CPI_VERSION=${VSPHERE_CPI_VERSION:-v1.2.1}
VSPHERE_CSI_VERSION=${VSPHERE_CSI_VERSION:-v2.1.0}
K8SCSI_CSI_ATTACHER_VERSION=${K8SCSI_CSI_ATTACHER_VERSION:-"v3.0.0"}
K8SCSI_CSI_NODE_DRIVER_VERSION=${K8SCSI_CSI_NODE_DRIVER_VERSION:-"v2.0.1"}
K8SCSI_CSI_PROVISIONER_VERSION=${K8SCSI_CSI_PROVISIONER_VERSION:-"v2.0.0"}
K8SCSI_CSI_RESIZER_VERSION=${K8SCSI_CSI_RESIZER_VERSION:-"v1.0.0"}
K8SCSI_CSI_LIVENESSPROBE_VERSION=${K8SCSI_CSI_LIVENESSPROBE_VERSION:-"v2.1.0"}
CLUSTER_AUTOSCALER_VERSION=${CLUSTER_AUTOSCALER_VERSION:-v1.8.5}
DNS_NODE_CACHE_VERSION=${DNS_NODE_CACHE_VERSION:-1.21.2}
CERTGEN_VERSION=${CERTGEN_VERSION:-v1.0}
HARVESTER_VERSION=${HARVESTER_VERSION:-v0.1.1-multiarch}
LONGHORN_REGISTRAR_VERSION=${LONGHORN_REGISTRAR_VERSION:-v2.3.0}
LONGHORN_RESIZER_VERSION=${LONGHORN_PROVISIONER_VERSION:-v1.2.0}
LONGHORN_PROVISIONER_VERSION=${LONGHORN_PROVISIONER_VERSION:-v2.1.2}
LONGHORN_ATTACHER_VERSION=${LONGHORN_ATTACHER_VERSION:-v3.2.1}

CILIUM_CHART_VERSION=${CILIUM_CHART_VERSION:-"1.10.404"}
CANAL_CHART_VERSION=${CANAL_CHART_VERSION:-"v3.20.1-build2021111904"}
CALICO_CHART_VERSION=${CALICO_CHART_VERSION:-"v3.20.201"}
CALICO_CRD_CHART_VERSION=${CALICO_CRD_CHART_VERSION:-"v1.0.202"}
COREDNS_CHART_VERSION=${COREDNS_CHART_VERSION:-"1.16.401-build2021111901"}
NGINX_INGRESS_CHART_VERSION=${NGINX_INGRESS_CHART_VERSION:-"4.0.306"}
METRICS_SERVER_CHART_VERSION=${METRICS_SERVER_CHART_VERSION:-"v3.20.1-build2021111904"}
MULTUS_CHART_VERSION=${MULTUS_CHART_VERSION:-"v3.20.1-build2021111904"}
VSPHERE_CPI_CHART_VERSION=${VSPHERE_CPI_CHART_VERSION:-"1.0.000"}
VSPHERE_CSI_CHART_VERSION=${VSPHERE_CSI_CHART_VERSION:-"2.1.000"}
HARVESTER_CLOUD_PROVIDER_CHART_VERSION=${HARVESTER_CLOUD_PROVIDER_CHART_VERSION:-"0.1.300"}
HARVESTER_CSI_DRIVER_CHART_VERSION=${HARVESTER_CSI_DRIVER_CHART_VERSION:-"0.1.400"}


GIT_TAG="${VERSION}"

if [[ "${VERSION}" =~ ^v([0-9]+)\.([0-9]+)(\.[0-9]+)?([-+].*)?$ ]]; then
    VERSION_MAJOR=${BASH_REMATCH[1]}
    VERSION_MINOR=${BASH_REMATCH[2]}
fi

DOCKERIZED_VERSION="${VERSION/+/-}" # this mimics what kubernetes builds do
